persistence:
  storageSize: 1Gi

postgres:
  user: gx_user
  password: gx_user_password
  database: demo

images:
  gxAgent: greatexpectations/agent:latest
  datadocs: oswaldodocker/datadocs:v3
  postgres: postgres:15.3-alpine
  mlflow: oswaldodocker/mlflow:v3
  notebook: oswaldodocker/notebook:v3

# These should be empty by default and filled via --set-file or environment variables
gxCloud:
  organizationId: ""
  accessToken: ""
